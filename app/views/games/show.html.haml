%p#notice= notice
= render @game
%div
  = link_to "Edit this game", edit_game_path(@game)
  |
  \#{link_to "Back to games", games_path}
  
  \#{button_to "Destroy this game", game_path(@game), method: :delete}
